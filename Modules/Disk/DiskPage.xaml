<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinCarePro.Views.DiskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:WinCarePro.Models"
    xmlns:vm="using:WinCarePro.ViewModels"
    xmlns:views="using:WinCarePro.Views"
    mc:Ignorable="d">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" HorizontalContentAlignment="Stretch" VerticalContentAlignment="Top">
        <Grid Padding="24,20" HorizontalAlignment="Stretch">
            <VisualStateManager.VisualStateGroups>
                <VisualStateGroup x:Name="LayoutVisualStates">
                    <!-- Wide Layout: Viewport Width >= 800px -->
                    <VisualState x:Name="WideState">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="800" />
                        </VisualState.StateTriggers>
                    </VisualState>
                    <!-- Narrow Layout: Viewport Width < 800px -->
                    <VisualState x:Name="NarrowState">
                        <VisualState.StateTriggers>
                            <AdaptiveTrigger MinWindowWidth="0" />
                        </VisualState.StateTriggers>
                        <VisualState.Setters>
                            <!-- Space Analyzer Header Stacking -->
                            <Setter Target="SpaceAnalyzerHeaderCol0.Width" Value="*" />
                            <Setter Target="SpaceAnalyzerHeaderCol1.Width" Value="0" />
                            <Setter Target="SpaceAnalyzerHeaderText.(Grid.ColumnSpan)" Value="2" />
                            <Setter Target="SpaceAnalyzerHeaderActions.(Grid.Row)" Value="1" />
                            <Setter Target="SpaceAnalyzerHeaderActions.(Grid.Column)" Value="0" />
                            <Setter Target="SpaceAnalyzerHeaderActions.(Grid.ColumnSpan)" Value="2" />
                            <Setter Target="SpaceAnalyzerHeaderActions.Margin" Value="0,8,0,0" />
                            <Setter Target="SpaceAnalyzerHeaderActions.HorizontalAlignment" Value="Left" />

                            <!-- Duplicate Finder Header Stacking -->
                            <Setter Target="DuplicateFinderHeaderCol0.Width" Value="*" />
                            <Setter Target="DuplicateFinderHeaderCol1.Width" Value="0" />
                            <Setter Target="DuplicateFinderHeaderText.(Grid.ColumnSpan)" Value="2" />
                            <Setter Target="DuplicateFinderHeaderActions.(Grid.Row)" Value="1" />
                            <Setter Target="DuplicateFinderHeaderActions.(Grid.Column)" Value="0" />
                            <Setter Target="DuplicateFinderHeaderActions.(Grid.ColumnSpan)" Value="2" />
                            <Setter Target="DuplicateFinderHeaderActions.Margin" Value="0,12,0,0" />
                            <Setter Target="DuplicateFinderHeaderActions.HorizontalAlignment" Value="Left" />
                        </VisualState.Setters>
                    </VisualState>
                </VisualStateGroup>
            </VisualStateManager.VisualStateGroups>

            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <!-- Page Header -->
            <Grid Grid.Row="0" Margin="0,0,0,16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0" Spacing="4">
                    <StackPanel Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                        <Border Background="{StaticResource CyberAccentGradient}" CornerRadius="10" Padding="8">
                            <FontIcon Glyph="&#xE7F1;" FontSize="18" Foreground="White"/>
                        </Border>
                        <TextBlock Text="Disk &amp; Storage Center" Style="{StaticResource PageHeaderTitleStyle}" VerticalAlignment="Center"/>
                        <Border Background="{ThemeResource AppBadgeGoodBg}" BorderBrush="{ThemeResource AppBadgeGoodBorder}" BorderThickness="1" CornerRadius="8" Padding="8,3" VerticalAlignment="Center">
                            <StackPanel Orientation="Horizontal" Spacing="5">
                                <Ellipse Width="6" Height="6" Fill="{ThemeResource AppBadgeGoodFg}"/>
                                <TextBlock Text="S.M.A.R.T Shield Active" FontSize="11" FontWeight="Bold" Foreground="{ThemeResource AppBadgeGoodFg}"/>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                    <TextBlock Text="Monitor drive SMART attributes, analyze allocation weight, and safely clean duplicate files." Style="{StaticResource PageHeaderSubtitleStyle}"/>
                </StackPanel>
                <Button Grid.Column="1" Click="OnCleanEmptyFoldersClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" Style="{StaticResource StandardSecondaryButtonStyle}" Height="36" Padding="14,0" VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Spacing="6">
                        <FontIcon Glyph="&#xE8B7;" FontSize="13"/>
                        <TextBlock Text="Clean Empty Folders" FontSize="12" FontWeight="SemiBold"/>
                    </StackPanel>
                </Button>
            </Grid>

            <Pivot x:Name="DiskPivot" Grid.Row="1" SelectionChanged="OnPivotSelectionChanged">
                <!-- Tab 1: Disk Overview -->
                <PivotItem Header="Disk Overview">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,16" Style="{StaticResource GlassmorphismCardStyle}" Padding="16">
                            <TextBlock Text="Local storage volumes health, physical S.M.A.R.T attributes, and hardware controllers status." FontSize="12.5" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                        </Border>

                        <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.Drives, Mode=OneWay}" SelectionMode="None">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <Border Style="{StaticResource GlassmorphismCardStyle}" Margin="0,0,0,12" Padding="20">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>

                                            <Border Width="44" Height="44" Background="#1510B981" CornerRadius="22" VerticalAlignment="Center" Margin="0,0,16,0">
                                                <FontIcon Glyph="&#xE7F1;" FontSize="18" Foreground="{StaticResource PrimaryAccentBrush}" />
                                            </Border>

                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="4">
                                                <TextBlock Text="{Binding Model}" FontWeight="Bold" FontSize="15" />
                                                <StackPanel Orientation="Horizontal" Spacing="16">
                                                    <TextBlock Text="{Binding Name}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                                    <TextBlock Text="{Binding Interface}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                                    <TextBlock Text="{Binding TemperatureFormatted}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                                </StackPanel>
                                            </StackPanel>

                                            <StackPanel Grid.Column="2" VerticalAlignment="Center" HorizontalAlignment="Right" Spacing="8">
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Spacing="8">
                                                    <Border Background="#1510B981" CornerRadius="4" Padding="8,4">
                                                        <TextBlock Text="{Binding HealthStatus}" Foreground="#FF10B981" FontWeight="Bold" FontSize="12"/>
                                                    </Border>
                                                </StackPanel>
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </Grid>
                </PivotItem>

                <!-- Tab 2: Disk Space Analyzer -->
                <PivotItem Header="Space Analyzer">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,16" Style="{StaticResource GlassmorphismCardStyle}" Padding="16">
                            <Grid x:Name="SpaceAnalyzerHeaderGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition x:Name="SpaceAnalyzerHeaderRow0" Height="Auto" />
                                    <RowDefinition x:Name="SpaceAnalyzerHeaderRow1" Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="SpaceAnalyzerHeaderCol0" Width="*" />
                                    <ColumnDefinition x:Name="SpaceAnalyzerHeaderCol1" Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel x:Name="SpaceAnalyzerHeaderText" VerticalAlignment="Center">
                                    <TextBlock Text="Analyze disk space allocation across large directories and user folders." FontSize="12.5" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    <!-- Breadcrumb Navigation Bar -->
                                    <StackPanel Orientation="Horizontal" Spacing="6" Margin="0,8,0,0">
                                        <TextBlock Text="Path:" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" VerticalAlignment="Center"/>
                                        <TextBlock Text="{x:Bind ViewModel.CurrentAnalysisPath, Mode=OneWay}" FontSize="11" FontWeight="Bold" Foreground="{StaticResource PrimaryAccentBrush}" VerticalAlignment="Center"/>
                                        <Button Content="Go Up" Click="OnGoUpDirectoryClick" Visibility="{x:Bind ViewModel.CanGoUp, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}" FontSize="10" Padding="8,2" Margin="6,0,0,0"/>
                                    </StackPanel>
                                </StackPanel>

                                <StackPanel x:Name="SpaceAnalyzerHeaderActions" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                                    <Button Content="Scan Directory" Click="OnAnalyzeSpaceClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Grid x:Name="SpaceAnalyzerSplitGrid" Grid.Row="1">
                            <Border x:Name="SpaceAnalyzerListBorder" Style="{StaticResource GlassmorphismCardStyle}" Padding="16">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>
                                    <TextBlock Grid.Row="0" Text="Allocation Directory Weight (Double-click folder card to Zoom In)" FontSize="15" FontWeight="Bold" Margin="0,0,0,12" />

                                    <ListView Grid.Row="1" ItemsSource="{x:Bind ViewModel.StorageItems, Mode=OneWay}" SelectionMode="None">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Grid Padding="8,10" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" DoubleTapped="StorageItem_DoubleTapped" Background="Transparent">
                                                    <Grid.ColumnDefinitions>
                                                        <ColumnDefinition Width="Auto" />
                                                        <ColumnDefinition Width="*" />
                                                        <ColumnDefinition Width="140" />
                                                        <ColumnDefinition Width="Auto" />
                                                    </Grid.ColumnDefinitions>
                                                    <FontIcon Glyph="{Binding IconGlyph}" FontSize="16" Foreground="{StaticResource PrimaryAccentBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                                                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="0,0,12,0">
                                                        <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="13"/>
                                                        <TextBlock Text="{Binding Path}" FontSize="10" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextTrimming="CharacterEllipsis"/>
                                                    </StackPanel>
                                                    
                                                    <!-- Storage Visual Weight Progress Bar -->
                                                    <StackPanel Grid.Column="2" VerticalAlignment="Center" Margin="0,0,12,0" Spacing="2">
                                                        <ProgressBar Value="{Binding Percentage}" Maximum="100" Height="6" CornerRadius="3" Foreground="{StaticResource PrimaryAccentBrush}" />
                                                        <TextBlock Text="{Binding PercentageFormatted}" FontSize="10" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" HorizontalAlignment="Right" />
                                                    </StackPanel>

                                                    <TextBlock Grid.Column="3" Text="{Binding SizeFormatted}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource PrimaryAccentBrush}" VerticalAlignment="Center"/>
                                                </Grid>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </Grid>
                            </Border>
                        </Grid>
                    </Grid>
                </PivotItem>

                <!-- Tab 3: Duplicate Finder -->
                <PivotItem Header="Duplicate Finder">
                    <Grid Margin="0,16,0,0">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Border Grid.Row="0" Margin="0,0,0,16" Style="{StaticResource GlassmorphismCardStyle}" Padding="16">
                            <Grid x:Name="DuplicateFinderHeaderGrid">
                                <Grid.RowDefinitions>
                                    <RowDefinition x:Name="DuplicateFinderHeaderRow0" Height="Auto" />
                                    <RowDefinition x:Name="DuplicateFinderHeaderRow1" Height="Auto" />
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition x:Name="DuplicateFinderHeaderCol0" Width="*" />
                                    <ColumnDefinition x:Name="DuplicateFinderHeaderCol1" Width="Auto" />
                                </Grid.ColumnDefinitions>
                                <StackPanel x:Name="DuplicateFinderHeaderText" VerticalAlignment="Center">
                                    <TextBlock Text="Find identical files using 2-stage verification (Head Hash + SHA-256)." FontSize="12.5" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                    <!-- Quick Selection Tools Bar -->
                                    <StackPanel Orientation="Horizontal" Spacing="8" Margin="0,8,0,0">
                                        <TextBlock Text="Selection Tools:" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" VerticalAlignment="Center" Margin="0,0,4,0"/>
                                        <Button Content="Select All" Click="OnSelectAllClick" FontSize="11" Padding="6,2"/>
                                        <Button Content="Deselect All" Click="OnDeselectAllClick" FontSize="11" Padding="6,2"/>
                                        <Button Content="Keep Newest" Click="OnKeepNewestClick" FontSize="11" Padding="6,2"/>
                                        <Button Content="Keep Oldest" Click="OnKeepOldestClick" FontSize="11" Padding="6,2"/>
                                    </StackPanel>
                                </StackPanel>
                                <StackPanel x:Name="DuplicateFinderHeaderActions" Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Bottom">
                                    <Button Content="Scan Duplicates" Click="OnScanDuplicatesClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}"/>
                                    <Button Content="Delete Selected" Style="{StaticResource AccentButtonStyle}" Background="{StaticResource PrimaryAccentGradient}" Foreground="White" Click="OnDeleteDuplicatesClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}"/>
                                </StackPanel>
                            </Grid>
                        </Border>

                        <Border Grid.Row="1" Style="{StaticResource GlassmorphismCardStyle}" Padding="16">
                            <ListView ItemsSource="{x:Bind ViewModel.DuplicateGroups, Mode=OneWay}" SelectionMode="None">
                                <ListView.ItemTemplate>
                                    <DataTemplate>
                                        <StackPanel Margin="0,0,0,16">
                                            <TextBlock Text="{Binding SizeFormatted}" FontWeight="Bold" FontSize="13" Foreground="{StaticResource PrimaryAccentBrush}" Margin="4,0,0,6"/>
                                            <ItemsControl ItemsSource="{Binding Items}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate>
                                                        <Grid Padding="24,4" BorderThickness="0,0,0,1" BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}">
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="Auto" />
                                                                <ColumnDefinition Width="*" />
                                                                <ColumnDefinition Width="Auto" />
                                                            </Grid.ColumnDefinitions>
                                                            <CheckBox IsChecked="{Binding IsSelectedForDeletion, Mode=TwoWay}" VerticalAlignment="Center" Margin="0,0,8,0" MinWidth="20" />
                                                            <StackPanel Grid.Column="1" VerticalAlignment="Center">
                                                                <TextBlock Text="{Binding Name}" FontWeight="Bold" FontSize="12"/>
                                                                <TextBlock Text="{Binding Path}" FontSize="10" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}"/>
                                                            </StackPanel>
                                                            <TextBlock Grid.Column="2" Text="{Binding LastModifiedFormatted}" FontSize="11" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" VerticalAlignment="Center"/>
                                                        </Grid>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                        </Border>
                    </Grid>
                </PivotItem>
            </Pivot>

            <!-- Activity Log & Diagnostics Console Expander -->
            <Expander Grid.Row="2" Header="Disk Tools Operations Console" HorizontalAlignment="Stretch" Margin="0,16,0,0" IsExpanded="False">
                <Border Background="{ThemeResource AppTerminalBackground}" CornerRadius="6" Padding="12" Height="140">
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
                        <TextBlock Text="{x:Bind ViewModel.ConsoleOutput, Mode=OneWay}" FontFamily="Consolas, Cascadia Code, Courier New" FontSize="11.5" Foreground="{ThemeResource AppTerminalForeground}" TextWrapping="Wrap"/>
                    </ScrollViewer>
                </Border>
            </Expander>
        </Grid>
    </ScrollViewer>
</Page>
