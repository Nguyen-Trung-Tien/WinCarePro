<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="WinCarePro.Views.SecurityPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:WinCarePro.ViewModels"
    xmlns:local="using:WinCarePro.Views"
    mc:Ignorable="d">

    <Grid Padding="24">
        <Grid.ChildrenTransitions>
            <TransitionCollection>
                <EntranceThemeTransition FromHorizontalOffset="0" FromVerticalOffset="20" />
            </TransitionCollection>
        </Grid.ChildrenTransitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid Grid.Row="0" Margin="0,0,0,24">
            <StackPanel>
                <TextBlock Text="Security &amp; Privacy Shield" FontSize="28" FontWeight="ExtraBold" CharacterSpacing="-10" />
                <TextBlock Text="Monitor local security components, verify hardware platform trust levels, and tune privacy profiles." Style="{ThemeResource BodyTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" Margin="0,4,0,0" />
            </StackPanel>
        </Grid>

        <!-- Main Body Pivots -->
        <Pivot Grid.Row="1">
            <!-- Security Center Tab -->
            <PivotItem Header="Security Center">
                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="0,16,16,16" Padding="20">
                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <!-- Left: Status Indicators -->
                        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Spacing="24">
                                <!-- Security Score Gauge -->
                                <Border Style="{StaticResource GradientBorderCardStyle}" Padding="24">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Grid Grid.Column="0" Width="140" Height="140" Margin="0,0,24,0">
                                            <Ellipse Width="124" Height="124" Stroke="{ThemeResource ControlStrokeColorDefaultBrush}" StrokeThickness="6" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.5"/>
                                            <ProgressRing Value="{x:Bind ViewModel.SecurityScore, Mode=OneWay}" Width="130" Height="130" Foreground="{x:Bind GetScoreBrush(ViewModel.SecurityScore), Mode=OneWay}" Background="Transparent" HorizontalAlignment="Center" VerticalAlignment="Center" />
                                            <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Spacing="2">
                                                <TextBlock Text="{x:Bind ViewModel.SecurityScore, Mode=OneWay}" FontSize="32" FontWeight="ExtraBold" HorizontalAlignment="Center" Foreground="{x:Bind GetScoreBrush(ViewModel.SecurityScore), Mode=OneWay}" CharacterSpacing="-20"/>
                                                <TextBlock Text="Security Score" FontSize="9.5" FontWeight="Bold" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" HorizontalAlignment="Center" />
                                            </StackPanel>
                                        </Grid>

                                        <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="8">
                                            <TextBlock Text="System Protection Level" FontSize="18" FontWeight="Bold" />
                                            <Border Background="{x:Bind GetScoreBadgeBackground(ViewModel.SecurityScore), Mode=OneWay}" HorizontalAlignment="Left" Padding="10,5" CornerRadius="6">
                                                <TextBlock Text="{x:Bind GetStatusText(ViewModel.SecurityScore), Mode=OneWay}" FontSize="11" FontWeight="Bold" Foreground="{x:Bind GetScoreBrush(ViewModel.SecurityScore), Mode=OneWay}" />
                                            </Border>
                                            <TextBlock Text="Ensures active antivirus defenses, firewall protection, platform integrity policies, and prevents unauthorized startup exposures." TextWrapping="Wrap" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" LineHeight="16" />

                                            <StackPanel Orientation="Horizontal" Spacing="12" Margin="0,4,0,0">
                                                <Button Content="Re-Scan Security" Style="{StaticResource DefaultButtonStyle}" Click="OnScanClick" IsEnabled="{x:Bind IsNot(ViewModel.IsScanning), Mode=OneWay}" CornerRadius="6" FontSize="11.5" Padding="12,6" />
                                                <ProgressRing IsActive="{x:Bind ViewModel.IsScanning, Mode=OneWay}" Width="20" Height="20" VerticalAlignment="Center" />
                                            </StackPanel>
                                        </StackPanel>
                                    </Grid>
                                </Border>

                                <!-- Hardware & Service Security Checklists -->
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Defense Status Checklists" FontSize="16" FontWeight="Bold" />

                                    <!-- Antivirus -->
                                    <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#1510B981" CornerRadius="8" Margin="0,0,12,0">
                                                <FontIcon Glyph="&#xE73A;" Foreground="#FF10B981" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="Antivirus Protection" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Text="{x:Bind ViewModel.AntivirusStatus, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="{x:Bind GetAvBadgeBg(ViewModel.AntivirusStatus), Mode=OneWay}" Padding="10,5" CornerRadius="6" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind GetAvLabel(ViewModel.AntivirusStatus), Mode=OneWay}" FontSize="10.5" FontWeight="Bold" Foreground="{x:Bind GetAvColor(ViewModel.AntivirusStatus), Mode=OneWay}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- Firewall -->
                                    <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#153B82F6" CornerRadius="8" Margin="0,0,12,0">
                                                <FontIcon Glyph="&#xE814;" Foreground="#FF3B82F6" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="Windows Defender Firewall" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Text="{x:Bind ViewModel.FirewallStatusText, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="{x:Bind GetFirewallBadgeBg(ViewModel.IsFirewallActive), Mode=OneWay}" Padding="10,5" CornerRadius="6" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind GetFirewallLabel(ViewModel.IsFirewallActive), Mode=OneWay}" FontSize="10.5" FontWeight="Bold" Foreground="{x:Bind GetFirewallColor(ViewModel.IsFirewallActive), Mode=OneWay}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- Secure Boot -->
                                    <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#158B5CF6" CornerRadius="8" Margin="0,0,12,0">
                                                <FontIcon Glyph="&#xE7F5;" Foreground="#FF8B5CF6" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="UEFI Secure Boot" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Text="{x:Bind ViewModel.SecureBootStatusText, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="{x:Bind GetFirewallBadgeBg(ViewModel.IsSecureBootEnabled), Mode=OneWay}" Padding="10,5" CornerRadius="6" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind GetFirewallLabel(ViewModel.IsSecureBootEnabled), Mode=OneWay}" FontSize="10.5" FontWeight="Bold" Foreground="{x:Bind GetFirewallColor(ViewModel.IsSecureBootEnabled), Mode=OneWay}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- TPM -->
                                    <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="Auto" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#1506B6D4" CornerRadius="8" Margin="0,0,12,0">
                                                <FontIcon Glyph="&#xE950;" Foreground="#FF06B6D4" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="TPM (Trusted Platform Module)" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Text="{x:Bind ViewModel.TpmStatusText, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
                                            </StackPanel>
                                            <Border Grid.Column="2" Background="{x:Bind GetFirewallBadgeBg(ViewModel.IsTpmEnabled), Mode=OneWay}" Padding="10,5" CornerRadius="6" VerticalAlignment="Center">
                                                <TextBlock Text="{x:Bind GetFirewallLabel(ViewModel.IsTpmEnabled), Mode=OneWay}" FontSize="10.5" FontWeight="Bold" Foreground="{x:Bind GetFirewallColor(ViewModel.IsTpmEnabled), Mode=OneWay}" />
                                            </Border>
                                        </Grid>
                                    </Border>

                                    <!-- BitLocker -->
                                    <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto" />
                                                <ColumnDefinition Width="*" />
                                            </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="36" Height="36" Background="#15EC7299" CornerRadius="8" Margin="0,0,12,0">
                                                <FontIcon Glyph="&#xE785;" Foreground="#FFEC7299" FontSize="16" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                                            </Border>
                                            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
                                                <TextBlock Text="BitLocker Volume Encryption" FontWeight="Bold" FontSize="13" />
                                                <TextBlock Text="{x:Bind ViewModel.BitLockerStatus, Mode=OneWay}" FontSize="12" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />
                                            </StackPanel>
                                        </Grid>
                                    </Border>
                                </StackPanel>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Right: Warnings & Alerts -->
                        <Grid Grid.Column="1" RowSpacing="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Style="{StaticResource ModernCardStyle}" Padding="24">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="*" />
                                    </Grid.RowDefinitions>

                                    <TextBlock Grid.Row="0" Text="Discovered Security Alerts" FontSize="18" FontWeight="Bold" Margin="0,0,0,16" />

                                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                                        <StackPanel Spacing="10">
                                            <TextBlock Text="No security risks or vulnerabilities detected. Your PC is secure." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" Visibility="{x:Bind GetVisibility(ViewModel.SecurityAlerts.Count), Mode=OneWay}" Margin="0,16,0,0" TextAlignment="Center" />

                                            <ItemsControl ItemsSource="{x:Bind ViewModel.SecurityAlerts, Mode=OneWay}">
                                                <ItemsControl.ItemTemplate>
                                                    <DataTemplate x:DataType="x:String">
                                                        <Border BorderBrush="#20EF4444" BorderThickness="1.5" Background="#10EF4444" CornerRadius="10" Margin="0,0,0,8" Padding="14">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto" />
                                                                    <ColumnDefinition Width="*" />
                                                                </Grid.ColumnDefinitions>
                                                                <FontIcon Glyph="&#xE7BA;" FontSize="14" Foreground="#FFEF4444" Margin="0,0,12,0" VerticalAlignment="Center" />
                                                                <TextBlock Grid.Column="1" Text="{x:Bind}" TextWrapping="Wrap" FontSize="12" LineHeight="16" VerticalAlignment="Center" Foreground="#FFEF4444" FontWeight="SemiBold" />
                                                            </Grid>
                                                        </Border>
                                                    </DataTemplate>
                                                </ItemsControl.ItemTemplate>
                                            </ItemsControl>
                                        </StackPanel>
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <Border Grid.Row="1" Style="{StaticResource ModernCardStyle}" Padding="24">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Security Guidance" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
                                    <TextBlock Text="• Windows Antivirus and Firewall block active malware processes and unsolicited remote port access." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                    <TextBlock Text="• TPM chips securely store passwords and keys, which is a prerequisite for Windows 11 hardware trust." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                    <TextBlock Text="• Secure Boot prevents malicious rootkits from loading during early BIOS handoff cycles." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </PivotItem>

            <!-- Privacy Tuning Tab -->
            <PivotItem Header="Privacy Tuning">
                <Border BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" BorderThickness="1" Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" CornerRadius="0,16,16,16" Padding="20">
                    <Grid ColumnSpacing="24">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*" />
                            <ColumnDefinition Width="2*" />
                        </Grid.ColumnDefinitions>

                        <!-- Left: Toggle Switches -->
                        <ScrollViewer Grid.Column="0" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
                            <StackPanel Spacing="16">
                                <TextBlock Text="Windows Privacy Toggles" FontSize="18" FontWeight="Bold" Margin="0,0,0,8" />

                                <!-- Advertising ID -->
                                <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                            <TextBlock Text="Personalized Ads (Advertising ID)" FontWeight="Bold" FontSize="13.5" />
                                            <TextBlock Text="Disable to block applications from tracking your app usage for targeted ads." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="14" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.AdvertisingIdEnabled, Mode=TwoWay}" Toggled="OnAdvertisingIdToggled" VerticalAlignment="Center" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" />
                                    </Grid>
                                </Border>

                                <!-- Telemetry -->
                                <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                            <TextBlock Text="System Diagnostic Telemetry" FontWeight="Bold" FontSize="13.5" />
                                            <TextBlock Text="Restrict telemetry reports sent to Microsoft to Security-only levels." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="14" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.TelemetryEnabled, Mode=TwoWay}" Toggled="OnTelemetryToggled" VerticalAlignment="Center" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" />
                                    </Grid>
                                </Border>

                                <!-- Clipboard History -->
                                <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                            <TextBlock Text="Windows Clipboard History" FontWeight="Bold" FontSize="13.5" />
                                            <TextBlock Text="Saves multiple copied items to clipboard for later use. Disable to prevent caching copied credentials." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="14" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.ClipboardHistoryEnabled, Mode=TwoWay}" Toggled="OnClipboardToggled" VerticalAlignment="Center" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" />
                                    </Grid>
                                </Border>

                                <!-- Input Tracking / RestrictImplicitConsent -->
                                <Border Style="{StaticResource ModernCardStyle}" Padding="16">
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*" />
                                            <ColumnDefinition Width="Auto" />
                                        </Grid.ColumnDefinitions>
                                        <StackPanel VerticalAlignment="Center" Margin="0,0,16,0">
                                            <TextBlock Text="Personalization &amp; Typing Input Tracking" FontWeight="Bold" FontSize="13.5" />
                                            <TextBlock Text="Restrict Windows from logging typing history and personalization telemetry." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="14" />
                                        </StackPanel>
                                        <ToggleSwitch Grid.Column="1" IsOn="{x:Bind ViewModel.InputTrackingEnabled, Mode=TwoWay}" Toggled="OnInputTrackingToggled" VerticalAlignment="Center" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" />
                                    </Grid>
                                </Border>
                            </StackPanel>
                        </ScrollViewer>

                        <!-- Right: Actions & Trace Cleaning -->
                        <Grid Grid.Column="1" RowSpacing="24">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="*" />
                            </Grid.RowDefinitions>

                            <Border Grid.Row="0" Style="{StaticResource ModernCardStyle}" Padding="24">
                                <StackPanel Spacing="16">
                                    <TextBlock Text="Clear Activity Traces" FontSize="18" FontWeight="Bold" />
                                    <TextBlock Text="Manually erase cached system activity logs and clipboard files:" Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" />

                                    <Button Content="Wipe Clipboard Cache" Click="OnClearClipboardClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" CornerRadius="6" HorizontalAlignment="Stretch" Padding="14,8" />
                                    <Button Content="Clear Recent Files &amp; Run History" Click="OnClearRecentClick" IsEnabled="{x:Bind IsNot(ViewModel.IsBusy), Mode=OneWay}" CornerRadius="6" HorizontalAlignment="Stretch" Padding="14,8" />
                                </StackPanel>
                            </Border>

                            <Border Grid.Row="1" Style="{StaticResource ModernCardStyle}" Padding="24">
                                <StackPanel Spacing="12">
                                    <TextBlock Text="Privacy Guard Summary" FontSize="18" FontWeight="Bold" Margin="0,0,0,4" />
                                    <TextBlock Text="• WinCare Pro privacy settings configure official Windows Registry parameters. These apply instantly and do not run third party agent daemons." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                    <TextBlock Text="• Disabling telemetry restricts Windows updates from monitoring usage diagnostics, which may reduce background data usage." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                    <TextBlock Text="• Traces clearing deletes temporary shortcuts inside Explorer Recent files history." Style="{ThemeResource CaptionTextBlockStyle}" Foreground="{ThemeResource SystemControlPageTextBaseMediumBrush}" TextWrapping="Wrap" LineHeight="16" />
                                </StackPanel>
                            </Border>
                        </Grid>
                    </Grid>
                </Border>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>
